<controls:MetroWindow x:Class="Ruminoid.LIVE.Windows.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:local="clr-namespace:Ruminoid.LIVE.Windows"
                      xmlns:chrome="http://schemes.grabacr.net/winfx/2014/chrome"
                      xmlns:controls="http://schemes.grabacr.net/winfx/2014/controls"
                      xmlns:yDock="clr-namespace:YDock;assembly=YDock"
                      xmlns:visualIcon="clr-namespace:VisualIcon;assembly=VisualIcon"
                      mc:Ignorable="d"
                      FontFamily="Segoe UI"
                      Background="{DynamicResource ThemeBrushKey}"
                      Foreground="White"
                      UseLayoutRounding="True"
                      IsRestoringWindowPlacement="True"
                      TextOptions.TextFormattingMode="Display"
                      SnapsToDevicePixels="True" Height="300" Width="800"
                      ResizeMode="NoResize"
                      Title="LIVE"
                      WindowStartupLocation="CenterScreen"
                      Name="RootElement">

    <chrome:WindowChrome.Instance>
        <chrome:WindowChrome />
    </chrome:WindowChrome.Instance>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Name="WndIn">
            <Grid Name="WndOut" Background="Red" Height="50" Width="30"
                  HorizontalAlignment="Center"/>
        </Grid>
    </Grid>
</controls:MetroWindow>
